package simulator;

public class CPU {
	int[] instruction_indices;
	//read array of instructions
	//call search on loop for caches (address instruction)
	//call search on loop for caches (address data)
	// in either case go to memory (add startaddress)
	//write policy !!
}
